@docs/CLAUDE.md

<!--
Las reglas del proyecto viven en `docs/CLAUDE.md` y el diario de decisiones en
`docs/ESTADO.md`. Se movieron ahí el 2026-08-16 para que la raíz del repositorio se lea como
lo que el proyecto es —una herramienta con su licencia, su modelo de amenazas y sus ADRs— y no
como un cuaderno de trabajo.

Este fichero se queda en la raíz **solo** como puntero, porque es donde las herramientas de
desarrollo esperan encontrarlo: si desapareciera, las reglas dejarían de cargarse solas y eso
es exactamente el modo de fallo silencioso que este proyecto se dedica a evitar.
-->
